package org.apatheia.model

import org.scalatest.flatspec.AnyFlatSpec
import org.scalatest.matchers.should.Matchers

class ContactSpec extends AnyFlatSpec with Matchers {

  behavior of "Contact"

  it should "parse a byte array value into a Contact" in {}

  it should "not parse an invalid byte array value" in {}

  it should "be parsed into a byte array" in {}

}
